<navigation:PhoneApplicationPage 
    x:Class="PhoneMilk.Gui.AuthPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:navigation="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Navigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    SupportedOrientations="Portrait"
    mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="480" xmlns:browser="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.WebBrowser"
    Loaded="PhoneApplicationPage_Loaded">

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="170"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--This is the name of the application and page title-->
        <Grid Grid.Row="0" x:Name="TitleGrid">
            <TextBlock x:Name="ApplicationName" Text="WINMILK" Style="{StaticResource PhoneTextPageTitle1Style}"/>
            <TextBlock x:Name="ListName" Text="authenicate" Style="{StaticResource PhoneTextPageTitle2Style}"/>
        </Grid>

        <!--This section is empty. Place new content here Grid.Row="1"-->
        <Grid Grid.Row="1" x:Name="ContentGrid">
            <TextBlock FontFamily="Segoe WP" FontSize="20" Foreground="White" TextWrapping="Wrap" Margin="20,0,0,552" HorizontalAlignment="Left" Width="294" VerticalAlignment="Center" Name="AuthStatus">
                When authentication is complete touch the button on the left.
            </TextBlock>
            <Button Content="Done" Height="70" HorizontalAlignment="Left" Margin="320,6,0,0" Name="AuthDoneButton" VerticalAlignment="Top" Width="160" Click="AuthDoneButton_Click"/>
            <browser:WebBrowser HorizontalAlignment="Left" Margin="6,89,0,0" Name="webBrowser1" VerticalAlignment="Top" Height="535" Width="468" />
        </Grid>
    </Grid>
</navigation:PhoneApplicationPage>